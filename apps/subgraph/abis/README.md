# ABIs

These files are populated by `pnpm -F @yield-pilot/contracts compile` +
the `scripts/copy-abis.sh` helper. Expected files:

- `YieldVault.json`

Until the first compile runs, this directory only contains this README.
