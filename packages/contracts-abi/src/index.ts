// Runs `pnpm -F @yield-pilot/contracts-abi build` to regenerate from artifacts.
// Until the first compile, ABI arrays are empty — treat as forward-declared.

export const YieldVaultAbi = [] as const;
export const PaymasterAbi = [] as const;
export const MockUsdcAbi = [] as const;
export const MockAaveAbi = [] as const;
