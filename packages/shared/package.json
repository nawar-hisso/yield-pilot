{
  "name": "@yield-pilot/shared",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./env": "./src/env.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo '(shared) no lint configured'",
    "test": "echo '(shared) no tests configured'",
    "build": "echo '(shared) no build step — re-exported as source'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@yield-pilot/tsconfig": "workspace:*",
    "typescript": "^5.5.0"
  }
}
